#include<stdio.h>
int main()
{
int i,j,flag=0;
char s[100];
char a[100];
gets(a);
gets(s);
for(i=0,j=0;a[i]!='\0'||s[j]!='\0';i++,j++)
{
if(a[i]!=s[j])
{flag=1;
break;
}
}
if(flag==0)
printf("equal");
else
printf("unequal");
return 0;
}
