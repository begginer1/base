#include<stdio.h>
#include<string.h>
int main()
{
int i,c=0;
char ch[100];
gets(ch);
int n=strlen(ch);
for(i=0;i<n;i++)
{
    if((ch[i+1]==' '&&ch[i]>=97)&&(ch[i]<=122)||(ch[i]>=97&&ch[i]<=122)&&(ch[i+1]=='\0')){
    c++;
    printf("%d",i);}
}
printf("%d",c);
    return 0;
}
