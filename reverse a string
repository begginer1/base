#include<stdio.h>
#include<string.h>
int main()
{ int s,i,c;char temp
    char str[100];
    scanf("%s",str);
    c=strlen(s);
    for(i=0;i<c/2;i++)
       {temp=str[i];
    str[i]=str[(c-1)-i];
    str[(c-1)-i]=temp;
       }
    printf("%s",str);
    return 0;
}
